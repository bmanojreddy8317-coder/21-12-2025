#include<stdio.h>
int main()
{
    int mode,fee =0 ;
    char category;
    scanf("%d",&mode);
    scanf(" %c",&category);
    switch(mode){
        case 1:
        switch(category){
        case 'R':
        fee = 5000;
        break;
        case 'S':
        fee = 3000;
        break;
        default:
        printf("Invalid Catogery");
    }
    break;
    case 2:
    switch(category){
    case 'R':
    fee = 9000;
    break;
    case 'S':
    fee = 7000;
    break;
    default:
    printf("Invalid Catogery");
    }
    break;
    default:
    printf("Invalid Mode");
    }
    printf("%d",fee);
    return 0;
}
