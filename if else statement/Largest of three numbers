#include<stdio.h>
int main()
{
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    if(a>b && a>c)
    printf("%d is Largest",a);
    else if(b>c && b>a)
    printf("%d is Largest",b);
    else
    printf("%d is largest",c);
    return 0;
}
