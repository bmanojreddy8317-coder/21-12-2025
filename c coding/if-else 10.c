#include<stdio.h>
int main()
{
    int temperature;
    scanf("%d",&temperature);
    if(temperature>=30)
    {
        printf("it is hot");
    }
        else
        {
            printf("it is normal");
        }
        return 0;
}